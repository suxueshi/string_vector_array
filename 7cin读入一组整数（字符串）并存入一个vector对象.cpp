//#include<iostream>
//#include<vector>
//#include<string>
//using namespace std;
//
////int main() {	//读入一组整数
////	vector<int> vInt;		//元素类型为int的vector对象
////	int i;					//记录用户的输入值
////	
////	char cont = 'y';
////	while (cin >> i) {
////		vInt.push_back(i);	//向vector对象
////		cout << "您要继续吗（y or n）? " << endl;
////		cin >> cont;
////		if (cont != 'y' && cont != 'Y')
////			break;
////	}
////	for (auto mem : vInt)	//使用范围for循环语句遍历vInt中的每个元素
////		cout << mem << " ";	
////	return 0;
////}
//
//int main() {	//读入一组字符串
//	vector<string> vString;		//元素类型为string的vector对象
//	string s;					//记录用户的输入值
//
//	char cont = 'y';
//	while (cin >> s) {
//		vString.push_back(s);	//向vector对象
//		cout << "您要继续吗（y or n）? " << endl;
//		cin >> cont;
//		if (cont != 'y' && cont != 'Y')
//			break;
//	}
//	for (auto mem : vString)	//使用范围for循环语句遍历vInt中的每个元素
//		cout << mem << " ";
//	return 0;
//}